using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ITVComponents.Plugins.SelfRegistration
{
    /*/// <summary>
    /// Marker Attribute used to indicate that a class is able to self-registration. 
    /// </summary>
    /// <remarks>
    /// Using this attribute implies, that everly constructor of the targeted class must have a SelfRegistrationCallback parameter registrationCallback at the end of the parameter list
    /// Incorrect usage of the attribute will cause an exception when initializing a plugin
    /// </remarks>
    [AttributeUsage(AttributeTargets.Class, AllowMultiple = false, Inherited = true)]
    [Obsolete("Use IDeferredInit Interface instead!",true)]
    public class SelfRegisteredAttribute : Attribute
    {
    }

    /// <summary>
    /// Registration callback for plugins that are marked with a SelfRegistered Attribute
    /// </summary>
    /// <param name="plugin">the plugin instance</param>
    [Obsolete("Use IDeferredInit Interface instead", true)]
    public delegate void SelfRegistrationCallback(IPlugin plugin);*/
}
